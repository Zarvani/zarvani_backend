# Node.js Backend Project

This repository contains the backend of a project built with Node.js and several essential dependencies. It is designed to handle server-side functionality, including API routes, database connections, authentication, and more.

---

## Features

- Secure user authentication using JSON Web Tokens (JWT).
- Input validation using `validator`.
- Data encryption with `bcryptjs`.
- Secure and efficient handling of cookies with `cookie-parser`.
- Integration with Google services using `google-auth-library`.
- Environment variable management with `dotenv`.
- API data fetching using `axios`.
- Cross-Origin Resource Sharing (CORS) enabled.
- MongoDB database management with `mongoose`.

---

## Prerequisites

Make sure you have the following installed on your machine:

- [Node.js](https://nodejs.org/) (v16 or higher recommended)
- [MongoDB](https://www.mongodb.com/) (for database management)

---

## Installation

1. Clone the repository:
   ```bash
   git clone https://github.com/Profile-Hub/profile-be.git
   
   cd profile-be
   
   npm install
   npm run dev  //development run
   npm start    // run backend
